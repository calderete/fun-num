<h2>Lets get temperature</h2>

<p>For Farenhiet to Celsius or vice versa enter a temperature and what scale it is in such as "32" in the temperature field and "c" or "f" in the scale field</p>

<%= form_tag(f2c_convert_path) do %>
	<%= render partial: "forms" %>
<% end %>

<p>To convert standard temperatures into Kelvin enter the temperature and what scale it is in. FunNum will then convert it into Kelvin</p>

<%= form_tag(to_k_path) do %>
	<%= render partial: "forms" %>
<% end %>

<p>To convert Kelvin temperatures into standard temperatures, enter the degrees in kelvin and then the scale you wish to conver to</p>

<%= form_tag(from_k_path) do %>
	<%= render partial: "forms" %>
<% end %>



